from activations.torch import ActivationModule
import matplotlib.pyplot as plt


def show_functions(module, axis=None, first_n=None):
    """
    Will show all the functions present in the module
    """
    pass



def _get_activations(module):
    """
    Will show all the functions present in the module
    + retrieve name
    """
    found_instances = {}
    if isinstance(func, ActivationModule):
        found_instances[name] = func


if __name__ == '__main__':
    VAE = ...
    show_functions(encoder)
